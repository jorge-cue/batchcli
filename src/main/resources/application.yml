spring:
  application:
    name: batchcli

  datasource:
    url: jdbc:h2:mem:default
    driver-class-name: org.h2.Driver
    username: sa
    password: '{noop}'

logging:
  level:
    root: warn
    org.springdemo.batchcli: debug
